{
  "name": "clippip",
  "version": "1.0.0",
  "private": true,
  "description": "選択範囲・選択テキストを常時最前面のPicture-in-Pictureで一時表示するChrome拡張",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "check:i18n": "node scripts/check-locales.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
